# UnitConverter_03 STEP 5 — Harness · Rule · tdd-red Command 보고서

| 항목 | 내용 |
|------|------|
| **프로젝트** | UnitConverter_03 |
| **경로** | `c:\김정화\DEV\UnitConverter_03` |
| **작성일** | 2026-06-11 |
| **단계** | STEP 5 — 세션 3 Harness · `.cursorrules` · ECB 골격 · `/tdd-red` Command |
| **Phase** | Test Loop 준비 (구현·RED 전) |
| **참고** | [Report/03.REPORT.md](03.REPORT.md) · [Report/04.REPORT.md](04.REPORT.md) · `docs/PRD.md` |
| **Transcript** | [Prompting/05.Export-Transcript.md](../Prompting/05.Export-Transcript.md) |

---

## 1. 개요

세션 3 Test Loop 착수를 위해 **pytest Harness**, **`.cursorrules`(Domain·API·ECB·TDD·AI)**, **ECB 모듈 골격**, **`/tdd-red` Command**를 추가했다. 구현·테스트 본문은 아직 없음.

| 항목 | 상태 |
|------|------|
| pytest Harness (`pyproject.toml`, `src/`, `tests/`) | ✅ 골격 |
| `.cursorrules` (43줄) | ✅ |
| ECB 골격 (`entity/`, `component/`, `validate_lines`) | ✅ |
| `.cursor/commands/tdd-red.md` | ✅ |
| RED/GREEN 구현 | ⏳ 미착수 |

---

## 2. `.cursorrules` 요약

| 섹션 | 핵심 |
|------|------|
| **Domain** | `unit:value` 한 번 입력 · SC-1~3 · SSOT · grid 정의 |
| **API** | `validate_lines(grid) -> {status, failed_lines}` |
| **ECB** | Entity / Component / Boundary · 의존 방향 |
| **TDD** | RED→GREEN→REFACTOR · RED은 `tests/`만 |
| **AI** | 한국어 · Phase 선언 · commit은 요청 시만 |

---

## 3. Harness · ECB 구조

```
src/
├── entity/
│   ├── constants.py          # METER_TO_FEET, METER_TO_YARD
│   ├── length_input.py         # LengthInput
│   ├── conversion_line.py      # ConversionLine
│   └── validation_result.py    # ValidationResult (TypedDict)
├── component/
│   ├── parse.py                # parse(input_str) -> LengthInput
│   ├── validate.py             # validate_input(...) -> str | None
│   ├── convert.py              # convert(...) -> list[ConversionLine]
│   └── format.py               # format_lines(...) -> list[str]
└── validate_lines.py           # Boundary — validate_lines(grid)
tests/
└── test_validate_lines.py      # import만 (본문 없음)
```

---

## 4. `/tdd-red` Command 요약

| 항목 | 내용 |
|------|------|
| **진입** | `/tdd-red` — `validate_lines` RED 전용 |
| **Phase** | 응답 첫 줄 `[RED]` |
| **절차** | AAA (Arrange → Act → Assert) |
| **우선 TC** | T-VAL-PASS-01, T-VAL-FAIL-01, T-VAL-INC-01/02 |
| **금지** | `src/` 수정 · assert 완화 · skip · xfail |

---

## 5. pytest

```bash
python -m pytest tests/ -v
```

- **결과:** 0 items collected (테스트 본문 없음 — Harness import 정상)

---

## 6. 생성·변경 파일

| 경로 | 설명 |
|------|------|
| `pyproject.toml` | pytest `testpaths`, `pythonpath` |
| `src/__init__.py` | 빈 패키지 |
| `src/validate_lines.py` | Boundary API 시그니처 |
| `src/entity/*.py` | Entity·constants·ValidationResult |
| `src/component/*.py` | Component 시그니처 4종 |
| `tests/__init__.py` | 빈 패키지 |
| `tests/test_validate_lines.py` | import만 |
| `.cursorrules` | 세션 3 Rule |
| `.cursor/commands/tdd-red.md` | RED Command |
| `Report/05.REPORT.md` | 본 보고서 |
| `Prompting/05.Export-Transcript.md` | Export transcript |

---

## 7. 다음 단계

1. `/tdd-red`로 T-VAL-PASS-01 (SC-2 grid) RED 추가
2. GREEN — `validate_lines` 최소 구현
3. Component(parse/validate/convert/format) 순차 TDD
4. `UnitConverter.py` Boundary 연동 · REFACTOR
